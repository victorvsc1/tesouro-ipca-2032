name: Update Tesouro IPCA+ 2032

on:
  workflow_dispatch:
  schedule:
    - cron: '0 18 * * 1-5'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - run: python scripts/update.py
      - name: Commit feed
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/ipca-2032.json
          if git diff --cached --quiet; then echo 'No changes.'; else git commit -m 'Update Tesouro IPCA+ 2032 quote'; git push; fi
